import 'package:flutter/material.dart';

class ExampleWide extends StatelessWidget {
  const ExampleWide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Wide')),
    );
  }
}